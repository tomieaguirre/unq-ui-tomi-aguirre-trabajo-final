import styles from "./WordChain.module.css";

export default function WordChain() {
    return (
        <section className={styles.container}>
            <h1>Cadena de palabras</h1>
        </section>
    );
}